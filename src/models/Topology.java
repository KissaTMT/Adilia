package models;

public class Topology {
    private int windowSize;

    public int getWindowSize() {
        return this.windowSize;
    }

    public Topology(int windowSize) {
        this.windowSize = windowSize;
    }
}
